import React from 'react';
import { StyleSheet, View } from 'react-native';
const UiBlockHeightThird = ({ children }) => (React.createElement(View, { style: styles.container }, children));
export default UiBlockHeightThird;
const styles = StyleSheet.create({
    container: {
        width: '100%',
        height: '33.333%'
    }
});
//# sourceMappingURL=height-third.js.map